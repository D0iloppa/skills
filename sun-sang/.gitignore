.DS_Store
*.swp
node_modules/
mcp-server/node_modules/

# 학습 데이터는 데이터 홈(기본 ~/.sun-sang, SUNSANG_HOME으로 오버라이드)에 귀속되며
# 정상 동작 시 이 repo 안에는 생기지 않는다. 아래 항목들은 SUNSANG_HOME 오설정, 구버전
# 코드 실행, 수동 테스트 잔여물 등으로 혹시 스킬 폴더 안에 데이터가 생기는 사고를 막는
# 안전망이다 — repo는 코드 공유용, 데이터는 절대 커밋하지 않는다는 원칙을 지킨다.
mcp-server/data/
mcp-server/secrets/
docs/
onboard.lock
*.db
*.db-*
